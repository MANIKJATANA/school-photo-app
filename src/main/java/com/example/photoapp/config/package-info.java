/**
 * Spring configuration beans: SecurityConfig (JWT filter chain),
 * JpaConfig, S3Config (BlobStore wiring), AsyncConfig (TaskExecutor &amp;
 * scheduling). Configuration only — no business logic.
 */
package com.example.photoapp.config;
